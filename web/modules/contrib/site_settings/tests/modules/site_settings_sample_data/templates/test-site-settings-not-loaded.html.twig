{%- if site_settings is not empty -%}
  Site settings were automatically passed to the twig template (auto loading on)
{%- else -%}
  Site settings were not automatically passed to the template (auto loading is off)
{%- endif -%}
